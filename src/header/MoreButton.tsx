import { ReactElement } from "react";

export function MoreButton(): ReactElement {
	return (
		<div className="more-button">
			<span className="material-symbols-outlined">more_vert</span>
		</div>
	);
}